# Copy this file to .env in the backend directory before running locally or deploying

# --- Database configuration ---
# Provide either the individual values below or a single DATABASE_URL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=alok_library
DB_USER=postgres
DB_PASSWORD=postgres

# DATABASE_URL=postgres://user:password@host:5432/alok_library
# DB_SSL=true

# --- Server configuration ---
PORT=3000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:5173
SERVE_FRONTEND=false

# --- Authentication ---
JWT_SECRET=change-me
JWT_EXPIRES_IN=24h

# --- Firebase Cloud Messaging (FCM) ---
# Option 1: Full Service Account JSON (Recommended)
# Get your Firebase service account JSON from Firebase Console:
# 1. Go to Firebase Console > Project Settings > Service Accounts
# 2. Click "Generate new private key"
# 3. Copy the entire JSON content and paste it here as a single-line string
#    (or use a JSON minifier to convert it to a single line)
# Example: FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project",...}
# Leave empty to disable FCM notifications
FIREBASE_SERVICE_ACCOUNT=

# Option 2: Individual Credentials (Alternative - if you prefer not to use full JSON)
# Extract these values from your Firebase service account JSON:
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# Note: Private key should include the BEGIN/END markers and newlines (\n)
